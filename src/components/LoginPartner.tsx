import {  Box } from '@mui/material';
import Login from './LoginPage'

const LoginPartner= () => {

    return (
        <Box>
            <Login role="partner"/>
        </Box>
    );
}
export default LoginPartner;